\documentclass[sigconf,nonacm]{acmart}

\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{enumitem}

\newcommand{\ours}{\textsc{EvoLearning}}
\newcommand{\ep}{\mathrm{EP}}
\newcommand{\kes}{\mathrm{KES}}
\newcommand{\dkt}{\mathrm{DKT}}
\newcommand{\bc}{\mathrm{BC}}
\newcommand{\ppo}{\mathrm{PPO}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\argmax}{\operatornamewithlimits{argmax}}

\settopmatter{printacmref=false}
\setcopyright{none}
\renewcommand\footnotetextcopyrightpermission[1]{}
\pagestyle{plain}

\begin{document}
\setlist[itemize]{leftmargin=10pt}
\setlist[enumerate]{leftmargin=15pt}

\title{EvoLearning: Evolutionary Expert-Guided Reinforcement Learning for Personalized Learning Path Recommendation}

\author{Anonymous}
\affiliation{\institution{Anonymous Institution} \country{}}
\email{anonymous@anonymous.org}

\begin{abstract}
In simulator-based sequential recommendation, a learned user model enables offline policy optimization for multi-step recommendation sequences. Learning path recommendation (LPR)---recommending an ordered sequence of knowledge concepts to maximize a learner's mastery---is a prominent instance where the \emph{order} of recommendations critically determines outcomes. However, reinforcement learning (RL) approaches to LPR face a \emph{search space explosion}: the number of possible paths grows combinatorially with sequence length, reaching $10^{38}$ for typical curricula with $L=20$ steps, while rewards are sparse and available only at the sequence end. We propose \ours{}, a three-stage framework that addresses this via \emph{evolutionary pre-exploration}. First, an evolutionary algorithm with diversity-regularized selection synthesizes high-quality expert trajectories per learner---filling the critical gap that expert demonstrations do not naturally exist in educational data. Second, behavioral cloning (BC) compresses these per-instance solutions into a generalizable policy. Third, proximal policy optimization (PPO) fine-tunes beyond the evolutionary ceiling through online interaction. Experiments on two real-world datasets with 10 baselines demonstrate state-of-the-art effectiveness and up to $7\times$ faster convergence than vanilla PPO. The evolutionary pre-exploration paradigm generalizes to any simulator-augmented sequential recommendation setting where per-user optimal sequences differ and sequence length makes exhaustive search intractable.
\end{abstract}

\begin{CCSXML}
<ccs2012>
<concept><concept_id>10002951.10003317.10003347.10003350</concept_id><concept_desc>Information systems~Recommender systems</concept_desc><concept_significance>500</concept_significance></concept>
<concept><concept_id>10010147.10010178.10010179</concept_id><concept_desc>Computing methodologies~Reinforcement learning</concept_desc><concept_significance>500</concept_significance></concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Information systems~Recommender systems}
\ccsdesc[500]{Computing methodologies~Reinforcement learning}

\keywords{Sequential Recommendation, Learning Path Recommendation, Reinforcement Learning, Evolutionary Algorithms, Behavioral Cloning, Simulator-based Recommendation}

\maketitle

%% ============================================================
\section{Introduction}
%% ============================================================

Sequential recommendation---predicting the next item a user should interact with given their history---is a core problem in recommender systems~\cite{hidasi2016gru4rec}. Learning path recommendation (LPR) is a particularly challenging instance of this paradigm, where the recommended sequence of knowledge concepts (KCs) must be \emph{jointly} optimized: unlike movie or product recommendation where each item has independent utility, in education the \emph{order} of concepts critically determines learning outcomes due to prerequisite dependencies, knowledge transfer, and cognitive load~\cite{liu2019cseal,li2023gehrl,cheng2025knowlp}. This makes LPR a \emph{simulator-based sequential recommendation} problem, where a learned student model serves as the environment for evaluating recommendation strategies.

Recent work frames LPR as a reinforcement learning (RL) problem within a knowledge evolution simulator (KES)---typically a deep knowledge tracing (DKT) model~\cite{piech2015dkt} that predicts how a learner's mastery evolves as they practice concepts~\cite{liu2019cseal}. Methods such as CSEAL~\cite{liu2019cseal}, SRC~\cite{wang2023src}, GEHRL~\cite{li2023gehrl}, and DLELP/KnowLP~\cite{cheng2025knowlp} have demonstrated the viability of this paradigm.

However, existing RL-based LPR methods harbor a critical but overlooked assumption: \textbf{they require the simulator at inference time}. During path generation, methods like CSEAL~\cite{liu2019cseal} and GEHRL~\cite{li2023gehrl} feed each recommended concept back through the KES to obtain the updated mastery $\mathbf{h}_t$, which is then used to select the next concept. As Li et al.~\cite{li2024privileged} point out, this is fundamentally invalid in real deployment: the simulator's internal knowledge state reflects what \emph{would} happen if the student practiced---but the student has not yet practiced. Using $\mathbf{h}_t$ during inference is analogous to \emph{planning a study schedule while already knowing the exam answers}.

Removing this simulator dependency---requiring the policy to plan from only the initial mastery $\mathbf{h}_0$---makes the problem dramatically harder. The policy must commit to an \emph{entire path of $L$ concepts without any intermediate feedback}, a form of \textbf{blind planning}. The search space grows as $\binom{M}{L} \cdot L!$---exceeding $10^{30}$ for $M=123$ and $L=20$---and the reward is \emph{sparse}, arriving only after the full path is executed. Without intermediate mastery signals to guide exploration, a randomly initialized RL policy is overwhelmed by this combinatorial blind search.

Moreover, expert demonstrations for learning paths \emph{do not naturally exist in the data}. Student logs record what learners \emph{actually did}, not what they \emph{should have done}~\cite{rajeswaran2018dexterous,nair2018demonstrations}. Collecting optimal demonstrations would require controlled experiments with expert tutors for each individual learner---clearly infeasible at scale.

We propose \ours{}, a framework that resolves both challenges---blind planning and the demonstration gap---through \emph{evolutionary pre-exploration}. The KES, while forbidden at inference, remains available during training. We exploit this asymmetry: an evolutionary algorithm searches for high-quality paths within the KES \emph{per individual learner}, synthesizing the expert demonstrations that cannot be found in the data. These demonstrations are then distilled into a simulator-free policy that plans entirely from $\mathbf{h}_0$, and refined via an asymmetric actor-critic PPO where only the critic accesses the privileged $\mathbf{h}_t$.

\begin{enumerate}
    \item \textbf{Evolutionary Expert Generation.} For each training learner, an evolutionary algorithm efficiently searches the path space within the KES. A diversity-regularized greedy selection inspired by determinantal point processes (DPP)~\cite{kulesza2012dpp} yields $K$ diverse expert trajectories per learner, ensuring broad coverage of effective strategies.

    \item \textbf{Behavioral Cloning Warm-Start.} The expert trajectories are distilled into a neural policy via supervised learning. This compresses the per-instance evolutionary knowledge into a \emph{generalizable} mapping from learner states to concept recommendations, effectively reducing the search space from $\binom{M}{L}$ to the neighborhood of demonstrated strategies.

    \item \textbf{PPO Fine-Tuning.} Starting from the BC-initialized policy, PPO refines the strategy through online interaction with the KES. Because the policy already operates in a high-reward region, PPO's exploration is directed toward local improvements rather than global random search.
\end{enumerate}

Our main contributions are:
\begin{itemize}
    \item We introduce \ours{}, a framework that addresses the demonstration gap in educational recommendation by automatically synthesizing expert trajectories via evolutionary search, then distilling them into a generalizable policy through BC$\to$PPO fine-tuning.
    \item We propose a diversity-regularized expert selection mechanism that prevents mode collapse in evolutionary trajectories and improves BC coverage across diverse learner states.
    \item We conduct comprehensive experiments comparing 10 methods across 2 datasets with path lengths $L \in \{5, 10, 20\}$, demonstrating state-of-the-art performance and up to $7\times$ faster convergence.
    \item We identify three conditions---cheap simulator, combinatorial action space, per-instance heterogeneity---under which evolutionary pre-exploration is effective for simulator-based sequential recommendation, suggesting applicability beyond education to domains such as healthcare and finance.
\end{itemize}

%% ============================================================
\section{Related Work}
%% ============================================================

\subsection{Knowledge Tracing}

Knowledge tracing (KT) models estimate a learner's evolving mastery over time from interaction sequences. Deep Knowledge Tracing (DKT)~\cite{piech2015dkt} pioneered the use of recurrent neural networks for this task. Subsequent models include DKVMN~\cite{zhang2017dkvmn}, SAKT~\cite{pandey2019sakt}, AKT~\cite{ghosh2020akt}, and SAINT~\cite{choi2020saint}, which leverage attention mechanisms and memory networks for improved accuracy. In LPR, KT models serve dual roles: as the knowledge evolution simulator (KES) for RL training and as the mastery estimator at inference time.

\subsection{Learning Path Recommendation}

Early LPR methods relied on heuristic rules such as prerequisite ordering~\cite{chen2018prerequisite} and difficulty matching~\cite{liu2019cseal}. The CSEAL framework~\cite{liu2019cseal} introduced the KES paradigm, training an RL agent within a DKT simulator using actor-critic methods with cognitive navigation---restricting the action space to graph-related concepts. SRC~\cite{wang2023src} formulated LPR as a set-to-sequence problem with concept-aware ranking. GEHRL~\cite{li2023gehrl} proposed hierarchical RL with a goal-planning agent and a goal-achieving agent, incorporating graph-based candidate filtering. DLELP and KnowLP~\cite{cheng2025knowlp} employ a multi-agent system with prerequisite and similarity agents, using EDU-GraphRAG for knowledge graph construction. GRU4Rec~\cite{hidasi2016gru4rec}, originally designed for session-based recommendation, has been adapted for sequential learning path generation.

\subsection{Bridging Search and Policy Optimization}

The paradigm of initializing RL with demonstrations has been studied extensively in robotics. Rajeswaran et al.~\cite{rajeswaran2018dexterous} use human demonstrations to warm-start natural policy gradient for dexterous manipulation, and Nair et al.~\cite{nair2018demonstrations} augment replay buffers with demonstrations to overcome exploration in sparse-reward tasks. These works rely on \emph{pre-existing expert demonstrations} (e.g., human teleoperation), which are expensive to collect and domain-specific.

Our approach differs in a fundamental way: we \emph{generate} demonstrations automatically via evolutionary search, eliminating the need for human experts. This is made possible by a key property of the LPR domain: the KES provides a \emph{zero-cost, deterministic simulator}. Evaluating a candidate path requires only $L$ forward passes through a small LSTM, making per-instance evolutionary search tractable---each learner's optimal path can be approximated through 60 candidates over 50 generations in under one second. In contrast, robotics domains require expensive physical simulation, making per-instance search infeasible.

Hybrid evolutionary-gradient methods such as ERL~\cite{khadka2018erl} and population-based training~\cite{jaderberg2017pbt} integrate evolution into the RL training loop, but evolve the \emph{policy parameters} rather than generating per-instance demonstrations. Evolutionary strategies~\cite{salimans2017es} replace gradient descent entirely. Our approach occupies a different point in this design space: evolution operates at the \emph{solution level} (generating paths for individual learners), while gradient-based learning operates at the \emph{policy level} (generalizing across learners). This separation allows each component to operate in its natural regime.

%% ============================================================
\section{Preliminaries}
%% ============================================================

\subsection{Problem Formulation}

Let $\mathcal{K} = \{k_1, k_2, \ldots, k_M\}$ denote a set of $M$ knowledge concepts in a curriculum. A learner is characterized by their interaction history $\mathcal{H} = \{(c_1, r_1), (c_2, r_2), \ldots\}$ where $c_t \in \mathcal{K}$ is the practiced concept and $r_t \in \{0, 1\}$ is the binary correctness outcome at step $t$.

\begin{definition}[Learning Path Recommendation]
Given a learner's history $\mathcal{H}$, a set of target concepts $\mathcal{G} \subseteq \mathcal{K}$, and a path length $L$, the goal is to find an ordered sequence $\mathbf{p} = (p_1, p_2, \ldots, p_L) \in \mathcal{K}^L$ that maximizes the learner's mastery improvement on the targets $\mathcal{G}$.
\end{definition}

\subsection{Knowledge Evolution Simulator (KES)}

Following the CSEAL framework~\cite{liu2019cseal}, we employ a trained DKT model~\cite{piech2015dkt} as the KES. Given a history $\mathcal{H}$, the DKT produces a mastery vector $\mathbf{h} \in [0,1]^M$ where $h_i$ represents the estimated probability of correctly answering concept $k_i$.

The KES operates with a deterministic response model: when a learner practices concept $c$, the outcome is determined by
\begin{equation}
    r = \mathbf{1}[h_c > 0.5]
\end{equation}
where $h_c$ is the learner's current mastery of concept $c$. The interaction $(c, r)$ is appended to $\mathcal{H}$, and the DKT produces an updated mastery vector.

\subsection{Effectiveness Percentage (EP)}

The standard evaluation metric for LPR is the Effectiveness Percentage~\cite{liu2019cseal}, which measures the normalized mastery improvement on target concepts after executing a learning path:
\begin{equation}
    \ep = \frac{1}{|\mathcal{G}'|} \sum_{g \in \mathcal{G}'} \frac{E_g^{\text{end}} - E_g^{\text{start}}}{1 - E_g^{\text{start}}}
    \label{eq:ep}
\end{equation}
where $E_g^{\text{start}}$ and $E_g^{\text{end}}$ are the learner's mastery of target $g$ before and after the path, and $\mathcal{G}' = \{g \in \mathcal{G} : 1 - E_g^{\text{start}} > 0.01\}$ excludes near-mastered targets.

\subsection{Concept Prerequisite Graph}

We construct a concept graph $\mathcal{G}_c = (\mathcal{K}, \mathcal{E}_p, \mathcal{E}_s)$ where $\mathcal{E}_p$ denotes prerequisite edges and $\mathcal{E}_s$ denotes similarity edges. Following prior work~\cite{liu2019cseal,cheng2025knowlp}, we derive these from the DKT's cross-concept influence: for each concept $k_i$, we measure the change in mastery of all other concepts when $k_i$ is practiced correctly. An edge $(k_i, k_j) \in \mathcal{E}_p$ if the influence exceeds a threshold $\tau$.

%% ============================================================
\section{Method}

\begin{figure*}[htb!]
    \centering
    % Placeholder for framework figure
    \fbox{\parbox{0.95\textwidth}{\centering\vspace{3cm}\textbf{Figure placeholder:} EvoLearning three-stage pipeline\\[1em]
    \small Stage 1: Evo Expert Generation (per-learner search + DPP diversity) $\longrightarrow$ Stage 2: Behavioral Cloning ($\mathbf{h}_0$ state) $\longrightarrow$ Stage 3: Asymmetric PPO (actor: $\mathbf{h}_0$, critic: $\mathbf{h}_t$)\\[0.5em]
    Inference: actor only ($\mathbf{h}_0 \to$ path), no KES dependency\vspace{2cm}}}
    \caption{Overview of the \ours{} framework. Stage~1 generates diverse expert trajectories via evolutionary search with DPP-inspired diversity selection. Stage~2 distills these into a simulator-free policy via behavioral cloning using only the initial mastery $\mathbf{h}_0$. Stage~3 refines the policy through asymmetric PPO: the actor sees $\mathbf{h}_0$ (matching inference), while the critic receives the privileged evolving mastery $\mathbf{h}_t$ from the KES for more accurate value estimation. At deployment, only the actor is used---no simulator access required.}
    \label{fig:framework}
\end{figure*}
%% ============================================================

\ours{} consists of three stages, as illustrated in Figure~\ref{fig:framework}. We describe each stage and the overall training pipeline.

\subsection{Stage 1: Evolutionary Expert Generation}

For each training learner with history $\mathcal{H}$, targets $\mathcal{G}$, and initial mastery $\mathbf{h}_0$, we search for high-quality learning paths using an evolutionary algorithm.

\paragraph{Candidate Space.} We construct a candidate set $\mathcal{C}$ of size at most $C$ by combining the target concepts, their prerequisites and similar concepts from the concept graph, and zone-of-proximal-development (ZPD) concepts whose mastery is closest to 0.5:
\begin{equation}
    \mathcal{C} = \mathcal{G} \cup \bigcup_{g \in \mathcal{G}} \left(\text{prereq}(g) \cup \text{sim}_K(g)\right) \cup \text{ZPD}
\end{equation}

\paragraph{Evolutionary Search.} We maintain a population of $N_{\text{pop}}$ candidate paths, each of length $L$ drawn from $\mathcal{C}$. Over $N_{\text{gen}}$ generations, the population evolves through tournament selection, ordered crossover, and point mutation. The fitness of each path $\mathbf{p}$ is evaluated in the KES:
\begin{equation}
    f(\mathbf{p}) = \ep(\mathcal{H}, \mathbf{p}, \mathcal{G})
\end{equation}

\paragraph{Diversity-Regularized Expert Selection.} To prevent mode collapse in the expert set, we select $K$ diverse paths per learner using a greedy DPP-inspired procedure. Starting with the highest-fitness path, we iteratively add the path that maximizes:
\begin{equation}
    \text{score}(\mathbf{p}) = f(\mathbf{p}) - \lambda \cdot \max_{\mathbf{p}' \in \mathcal{S}} \text{sim}(\mathbf{p}, \mathbf{p}')
    \label{eq:dpp}
\end{equation}
where $\mathcal{S}$ is the currently selected set, $\text{sim}(\mathbf{p}, \mathbf{p}') = |\mathbf{p} \cap \mathbf{p}'| / L$ measures concept overlap, and $\lambda$ controls the quality--diversity trade-off. This greedy procedure mirrors the sequential selection in DPP MAP inference~\cite{kulesza2012dpp}, where the log-determinant objective decomposes into a quality term minus a redundancy penalty. While we use a simplified Jaccard-based similarity kernel rather than a full DPP kernel matrix, the objective structure---balancing item quality against pairwise redundancy---is equivalent.

\subsection{Stage 2: Behavioral Cloning}

The evolutionary expert trajectories provide state--action pairs for supervised learning. Consistent with the simulator-free actor design (Section~4.3), the BC state uses the \emph{initial} mastery $\mathbf{h}_0$, ensuring the actor never depends on KES feedback---not during BC training, PPO fine-tuning, or inference. For each expert $(\mathbf{h}_0, \mathcal{G}, \mathbf{p}, \text{EP})$, we construct $L$ training examples:
\begin{equation}
    \{(\mathbf{s}_t, a_t)\}_{t=0}^{L-1}, \quad \mathbf{s}_t = [\mathbf{h}_0 \| \mathbf{g} \| t/L]
\end{equation}
where $\mathbf{g} \in \{0,1\}^M$ is the target mask, $t/L$ provides step position, and $a_t = p_t$ is the expert's action at step $t$. The step index and the used-concept exclusion mask (applied during action selection) are the only signals that differentiate steps---the actor learns to plan an entire path from the initial knowledge state alone.

The policy $\pi_\theta$ is trained to minimize the cross-entropy loss:
\begin{equation}
    \mathcal{L}_{\bc} = -\frac{1}{|\mathcal{D}|} \sum_{(\mathbf{s}, a) \in \mathcal{D}} \log \pi_\theta(a \mid \mathbf{s})
\end{equation}

\subsection{Stage 3: PPO Fine-Tuning}

Starting from the BC-initialized policy, we apply PPO~\cite{schulman2017ppo} for online fine-tuning. At each episode, we sample a batch of learners and collect trajectories by interacting with the KES.

\paragraph{Simulator-Free Inference via Asymmetric Actor-Critic.}

A critical design consideration in KES-based LPR is whether the policy may access the simulator at inference time. As noted by Lee et al.~\cite{li2024privileged}, the simulator's internal knowledge state is unavailable in real deployment---a tutoring system cannot observe how a student's mastery evolves \emph{before} they actually practice. Relying on per-step simulator feedback during inference is akin to planning a study schedule while already knowing the exam answers.

We address this through an \emph{asymmetric actor-critic} architecture. The \textbf{actor} (policy) receives only the \emph{initial} mastery $\mathbf{h}_0$ at every step, ensuring KES-free inference:
\begin{equation}
    \mathbf{s}_t^{\text{actor}} = [\mathbf{h}_0 \| \mathbf{g} \| t/L], \quad a_t \in \mathcal{K} \setminus \{a_0, \ldots, a_{t-1}\}
\end{equation}
where $\mathbf{g} \in \{0,1\}^M$ is the target mask and $t/L$ provides step position. The actor distinguishes steps through the step index and the \emph{used concept mask} that excludes previously selected concepts from the action space. At deployment, the complete path is generated by $L$ forward passes through the actor alone---no simulator access required.

The \textbf{critic} (value function), used only during training, receives the \emph{privileged} evolving mastery $\mathbf{h}_t$ from the KES:
\begin{equation}
    \mathbf{s}_t^{\text{critic}} = [\mathbf{h}_t \| \mathbf{g} \| t/L]
\end{equation}
This asymmetry allows the critic to provide more accurate advantage estimates---it observes how each recommendation actually affects mastery---while the actor learns to plan without this privileged information. The actor-critic separation ensures that the deployed policy is fully \emph{simulator-free}: it takes a learner's initial knowledge state and target concepts as input, and outputs a complete learning path in a single pass.

\paragraph{Reward.} We use a sparse terminal reward computed via KES simulation:
\begin{equation}
    R_t = \begin{cases} \ep(\mathcal{H}, \mathbf{p}_{0:L}, \mathcal{G}) & \text{if } t = L-1 \\ 0 & \text{otherwise} \end{cases}
\end{equation}

\paragraph{Policy Optimization.} The PPO clipped surrogate objective is:
\begin{equation}
    \mathcal{L}_{\ppo} = -\E_t\left[\min\left(\rho_t \hat{A}_t,\ \text{clip}(\rho_t, 1\pm\epsilon) \hat{A}_t\right)\right]
\end{equation}
where $\rho_t = \pi_\theta(a_t|\mathbf{s}_t^{\text{actor}}) / \pi_{\theta_{\text{old}}}(a_t|\mathbf{s}_t^{\text{actor}})$ is the importance ratio and $\hat{A}_t = R_t + \gamma V(\mathbf{s}_{t+1}^{\text{critic}}) - V(\mathbf{s}_t^{\text{critic}})$ uses the privileged critic. The full loss includes a value function term and an entropy bonus:
\begin{equation}
    \mathcal{L} = \mathcal{L}_{\ppo} + c_v \cdot \mathcal{L}_{\text{value}} - c_e \cdot H[\pi_\theta]
\end{equation}

%% ============================================================
\section{Experiments}
%% ============================================================

\subsection{Datasets}

We evaluate on two widely-used educational datasets:

\begin{itemize}
    \item \textbf{ASSISTments 2009}~\cite{feng2009assistments}: An online mathematics tutoring platform. After preprocessing, the dataset contains 2,303 learners, 123 knowledge concepts, and 338K interactions.
    \item \textbf{Junyi Academy}~\cite{chang2015junyi}: A Chinese mathematics learning platform. We sample 10,000 learners (seed-controlled), yielding 39 knowledge concepts and 2.4M interactions.
\end{itemize}

Following the CSEAL protocol~\cite{liu2019cseal}, we split each dataset 50/50 into $\mathcal{D}_{\text{Sim}}$ (for DKT training) and $\mathcal{D}_{\text{Off}}$ (for RL training and evaluation). $\mathcal{D}_{\text{Off}}$ is further split 80/10/10 into training, validation, and test sets.

\subsection{Baselines}

We compare against 9 methods spanning 5 categories:

\begin{itemize}
    \item \textbf{Heuristic:} \textit{Random} (uniform concept sampling), \textit{Target-repeat} (cyclic repetition of target concepts), \textit{Rule-based} (topological sort of prerequisite graph).
    \item \textbf{Sequential:} \textit{GRU4Rec}~\cite{hidasi2016gru4rec} (GRU-based autoregressive model trained on expert trajectories via behavioral cloning).
    \item \textbf{RL:} \textit{PPO-vanilla}~\cite{schulman2017ppo} (PPO from random initialization).
    \item \textbf{Graph-enhanced RL:} \textit{CSEAL}~\cite{liu2019cseal} (A2C with cognitive navigation), \textit{GEHRL}~\cite{li2023gehrl} (hierarchical RL with goal planning), \textit{DLELP}~\cite{cheng2025knowlp} (PPO with similarity agent for plateau detection).
    \item \textbf{LLM-enhanced:} \textit{KnowLP}~\cite{cheng2025knowlp} (DLELP with EDU-GraphRAG LLM-generated concept graph).
\end{itemize}

All baselines are re-implemented under identical experimental conditions (same DKT model, data split, and evaluation protocol) for fair comparison.

\subsection{Implementation Details}

\paragraph{KES.} We train a DKT model~\cite{piech2015dkt} with LSTM on $\mathcal{D}_{\text{Sim}}$ using the pyKT framework. For ASSISTments 2009, the DKT uses embedding size 600 and achieves AUC 0.83. For Junyi, embedding size 256 with AUC 0.74.

\paragraph{Concept Graph.} We construct the prerequisite and similarity graphs from the DKT's cross-concept influence matrix with thresholds $\tau = 0.25$ (ASSISTments) and $\tau = 0.05$ (Junyi).

\paragraph{Evolutionary Search.} Population size $N_{\text{pop}} = 60$, generations $N_{\text{gen}} = 50$, candidate cap $C = 30$, DPP diversity parameter $\lambda = 0.5$, $K = 5$ diverse paths per learner.

\paragraph{Policy Network.} 2-layer MLP with hidden size 512 (ASSISTments) or 256 (Junyi), ReLU activations, shared backbone with separate policy and value heads.

\paragraph{Training.} BC: 3,000 epochs, Adam with $\text{lr}=10^{-3}$. PPO: 30,000 episodes, batch size 128, $\text{lr}=10^{-4}$, $\epsilon=0.15$, entropy coefficient 0.03, cosine LR schedule, 4 PPO epochs per batch, gradient clip 0.5.

\subsection{Main Results}

Table~\ref{tab:main} presents the EP scores across all datasets and path lengths.

\begin{table*}[t]
\centering
\caption{Test EP scores across datasets and path lengths ($L \in \{5, 10, 20\}$). Best in \textbf{bold}, second best \underline{underlined}. All RL methods trained for 30K episodes; mean over 3 seeds reported. Entries marked --- are pending.}
\label{tab:main}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}cl|ccc|c|c|ccc|c|c@{}}
\toprule
& & \multicolumn{3}{c|}{Heuristic} & Seq. & RL & \multicolumn{3}{c|}{Graph-enhanced RL} & LLM & \textbf{Ours} \\
Dataset & $L$ & Random & Target & Rule & GRU4Rec & PPO & CSEAL & GEHRL & DLELP & KnowLP & \textbf{\ours{}} \\
\midrule
\multirow{3}{*}{\shortstack[c]{\textbf{ASSIST09}\\{\scriptsize 123 KCs}\\{\scriptsize 116 test}}}
& 5  & $-$0.43 & $-$0.07 & $-$0.60 & +0.11 & \underline{+0.34} & +0.12 & +0.13 & --- & --- & \textbf{+0.37} \\
& 10 & ---     & ---     & ---     & ---   & ---   & ---   & ---   & --- & --- & --- \\
& 20 & ---     & ---     & ---     & ---   & ---   & ---   & ---   & --- & --- & --- \\
\midrule
\multirow{3}{*}{\shortstack[c]{\textbf{Junyi}\\{\scriptsize 39 KCs}\\{\scriptsize 500 test}}}
& 5  & $-$0.59 & +0.17   & $-$0.91 & +0.19 & +0.15 & +0.10 & ---   & --- & --- & --- \\
& 10 & ---     & ---     & ---     & ---   & ---   & ---   & ---   & --- & --- & --- \\
& 20 & ---     & ---     & ---     & ---   & ---   & ---   & ---   & --- & --- & --- \\
\midrule
% === Third dataset placeholder (uncomment when ready) ===
% \multirow{3}{*}{\shortstack[c]{\textbf{EdNet}\\{\scriptsize -- KCs}\\{\scriptsize -- test}}}
% & 5  & --- & --- & --- & --- & --- & --- & --- & --- & --- & --- \\
% & 10 & --- & --- & --- & --- & --- & --- & --- & --- & --- & --- \\
% & 20 & --- & --- & --- & --- & --- & --- & --- & --- & --- & --- \\
% \midrule
\bottomrule
\end{tabular}%
}
\end{table*}

\subsection{Convergence Analysis}

\begin{figure}[t]
    \centering
    \fbox{\parbox{0.95\columnwidth}{\centering\vspace{3cm}\textbf{Figure placeholder:} Convergence curves (Val EP vs. Episodes) for PPO-vanilla and EvoLearning\vspace{1cm}}}
    \caption{Validation EP during training on ASSISTments 2009 ($L=5$). \ours{} (BC-initialized) achieves higher EP from the first evaluation point and converges substantially faster than PPO-vanilla.}
    \label{fig:convergence}
\end{figure}

\subsection{Ablation Study}

\begin{table}[t]
\centering
\caption{Ablation study on ASSISTments 2009 ($L=5$). \textit{Placeholder for completed experiments.}}
\label{tab:ablation}
\begin{tabular}{@{}lcc@{}}
\toprule
Variant & Val EP & Test EP \\
\midrule
Evo experts only (no learning) & --- & --- \\
BC only (no PPO) & --- & --- \\
PPO only (no BC init) & --- & --- \\
BC + PPO (w/o DPP diversity) & --- & --- \\
\textbf{\ours{} (full)} & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

%% ============================================================
\section{Analysis}
%% ============================================================

\subsection{Search Space Reduction and BC Initialization}

The central design principle of \ours{} is \emph{progressive search space reduction}. We first quantify this reduction, then explain three mechanisms through which it accelerates PPO. Consider the path space for $M=123$ concepts:
\begin{center}
\begin{tabular}{@{}lrl@{}}
\toprule
$L$ & $\binom{M}{L} \cdot L!$ & Magnitude \\
\midrule
5 & $2.5 \times 10^{10}$ & $10^{10}$ \\
10 & $2.3 \times 10^{20}$ & $10^{20}$ \\
20 & $4.7 \times 10^{38}$ & $10^{38}$ \\
\bottomrule
\end{tabular}
\end{center}
For $L=20$, a random policy's probability of finding a near-optimal path is vanishingly small. \ours{} addresses this through three compression steps: (1)~the candidate set $\mathcal{C}$ reduces the per-step action space from $M$ to $C \ll M$ using graph structure; (2)~evolution searches within this reduced space, finding high-quality paths in $O(N_{\text{pop}} \times N_{\text{gen}})$ evaluations; (3)~BC compresses $K \times N_{\text{train}}$ expert paths into a neural policy that generalizes to unseen learners. The result is that PPO starts from a policy whose support is concentrated on a \emph{polynomially-sized} subset of the exponential path space.

\paragraph{(1) Escaping the exploration bottleneck.} In LPR, the reward is \emph{sparse}---only the terminal EP at step $L$ provides a learning signal, while all intermediate steps receive zero reward. A randomly initialized policy produces near-random paths with near-zero EP, providing negligible gradient signal. The BC-initialized policy, by contrast, already produces paths with positive EP from the first episode, generating informative gradients from the start.

\paragraph{(2) Mitigating compounding errors.} Standard BC suffers from distribution shift~\cite{ross2011dagger}: small errors compound across sequential decisions because the policy encounters states not seen during training. In our framework, PPO \emph{corrects} this shift through online interaction---the fine-tuning stage explicitly trains on the policy's own state distribution, closing the gap that pure BC leaves open. This positions our approach between pure imitation (BC) and pure exploration (PPO), combining the sample efficiency of the former with the asymptotic optimality of the latter.

\paragraph{(3) Biasing exploration toward high-reward regions.} The entropy-regularized PPO objective encourages exploration, but the \emph{direction} of exploration depends on the initial policy. A BC-initialized policy explores \emph{around} known-good strategies, while a random policy explores uniformly. In the $\binom{M}{L}$ path space, the high-reward region is vanishingly small; BC initialization ensures that PPO's exploration budget is spent in the vicinity of this region rather than in the vast low-reward space.

\subsection{Role of DPP Diversity}

A critical but subtle design choice is the DPP-based diverse expert selection (Eq.~\ref{eq:dpp}). Without diversity enforcement, evolutionary search converges to a narrow fitness peak, producing near-identical expert paths. When BC trains on such redundant demonstrations, the resulting policy \emph{mode-collapses}---it memorizes a single strategy rather than learning a generalizable mapping from states to actions.

DPP selection ensures that the $K$ trajectories per learner cover qualitatively different strategies (e.g., prerequisite-first vs.\ target-direct vs.\ ZPD-focused paths). This diversity in the training set acts as implicit regularization, producing a BC policy that generalizes across the state space rather than overfitting to one behavioral mode. We validate this in the ablation study (Table~\ref{tab:ablation}).

\subsection{Generalizability Beyond Education}

The evolutionary pre-exploration paradigm is not specific to education. We identify three conditions under which it is effective for \emph{simulator-augmented sequential recommendation}:
\begin{enumerate}
    \item \textbf{Cheap simulator.} A learned user model enables zero-cost trajectory evaluation, making per-instance evolutionary search tractable. In education, this is the KES (DKT); analogous simulators exist in healthcare (patient progression models) and finance (portfolio simulators).
    \item \textbf{Combinatorial sequence space.} The recommendation sequence is long enough ($L \geq 5$) that exhaustive or random search fails. This distinguishes LPR from next-item recommendation, where a single prediction suffices.
    \item \textbf{Per-user heterogeneity.} Optimal sequences differ across users, requiring personalized demonstrations rather than a single global expert policy. This is the norm in education (each learner has a unique knowledge state) and healthcare (each patient has a unique condition).
\end{enumerate}
When these conditions hold, evolutionary pre-exploration provides a principled way to synthesize expert demonstrations in domains where they cannot be collected from human experts, enabling the BC$\to$RL pipeline even without access to oracle policies.

%% ============================================================
\section{Conclusion}
%% ============================================================

We presented \ours{}, a framework for learning path recommendation that resolves the expert demonstration gap through evolutionary pre-exploration. By synthesizing diverse, high-quality trajectories per learner and distilling them into a neural policy via BC$\to$PPO, \ours{} reduces the effective search space from combinatorial to tractable, achieving state-of-the-art performance with significantly faster convergence. The underlying paradigm---evolutionary demonstration generation for simulator-based sequential recommendation---extends naturally to healthcare, finance, and other domains where per-user optimal sequences must be discovered without human expert supervision.

\paragraph{Limitations and Future Work.}

\textit{Computational cost.} Evolutionary expert generation requires $N_{\text{train}} \times N_{\text{pop}} \times N_{\text{gen}} \times L$ KES evaluations as a preprocessing step ($\sim$70 minutes for 921 learners on a single GPU). This cost is amortized (generated once, reused across runs) and parallelizable (per-learner searches are independent).

\textit{Candidate space sensitivity.} The candidate set $\mathcal{C}$ relies on data-driven graph structure and ZPD heuristics. Systematic sensitivity analysis across domains is left for future work.

\textit{Future directions.} Multi-objective optimization (balancing mastery with engagement), evaluation with human learners, and online adaptation of evolutionary experts as the policy improves.

%% ============================================================
%% References
%% ============================================================
\bibliographystyle{ACM-Reference-Format}
\begin{thebibliography}{25}

\bibitem{piech2015dkt}
C.~Piech, J.~Bassen, J.~Huang, S.~Ganguli, M.~Sahami, L.~Guibas, and J.~Sohl-Dickstein.
\newblock Deep knowledge tracing.
\newblock In \emph{Advances in Neural Information Processing Systems (NeurIPS)}, pages 505--513, 2015.

\bibitem{liu2019cseal}
Q.~Liu, S.~Tong, C.~Liu, H.~Zhao, E.~Chen, H.~Ma, and S.~Wang.
\newblock Exploiting cognitive structure for adaptive learning.
\newblock In \emph{Proceedings of the 25th ACM SIGKDD International Conference on Knowledge Discovery \& Data Mining}, pages 627--635, 2019.

\bibitem{li2023gehrl}
Q.~Li, W.~Xia, L.~Yin, J.~Shen, R.~Rui, W.~Zhang, X.~Chen, R.~Tang, and Y.~Yu.
\newblock Graph enhanced hierarchical reinforcement learning for goal-oriented learning path recommendation.
\newblock In \emph{Proceedings of the 32nd ACM International Conference on Information and Knowledge Management (CIKM)}, pages 1318--1327, 2023.

\bibitem{cheng2025knowlp}
X.~Cheng, Z.~Zhang, J.~Wang, L.~Fang, C.~He, Q.~Guan, S.~Pan, and W.~Luo.
\newblock Education-oriented graph retrieval-augmented generation for learning path recommendation.
\newblock \emph{arXiv preprint arXiv:2506.22303}, 2025.

\bibitem{wang2023src}
Y.~Wang, H.~Zhao, B.~Qin, and T.~Liu.
\newblock Set-to-sequence ranking-based concept-aware learning path recommendation.
\newblock In \emph{Proceedings of the AAAI Conference on Artificial Intelligence}, volume~37, pages 63--71, 2023.

\bibitem{schulman2017ppo}
J.~Schulman, F.~Wolski, P.~Dhariwal, A.~Radford, and O.~Klimov.
\newblock Proximal policy optimization algorithms.
\newblock \emph{arXiv preprint arXiv:1707.06347}, 2017.

\bibitem{hidasi2016gru4rec}
B.~Hidasi, A.~Karatzoglou, L.~Baltrunas, and D.~Tikk.
\newblock Session-based recommendations with recurrent neural networks.
\newblock In \emph{Proceedings of the 4th International Conference on Learning Representations (ICLR)}, 2016.

\bibitem{zhang2017dkvmn}
J.~Zhang, X.~Shi, I.~King, and D.-Y.~Yeung.
\newblock Dynamic key-value memory networks for knowledge tracing.
\newblock In \emph{Proceedings of the 26th International Conference on World Wide Web (WWW)}, pages 765--774, 2017.

\bibitem{pandey2019sakt}
S.~Pandey and G.~Karypis.
\newblock A self-attentive model for knowledge tracing.
\newblock In \emph{Proceedings of the 12th International Conference on Educational Data Mining (EDM)}, 2019.

\bibitem{ghosh2020akt}
A.~Ghosh, N.~Heffernan, and A.~S.~Lan.
\newblock Context-aware attentive knowledge tracing.
\newblock In \emph{Proceedings of the 26th ACM SIGKDD International Conference on Knowledge Discovery \& Data Mining}, pages 2330--2339, 2020.

\bibitem{choi2020saint}
Y.~Choi, Y.~Lee, J.~Shin, J.~Cho, S.~Park, S.~Lee, and J.~Bae.
\newblock Towards an appropriate query, key, and value computation for knowledge tracing.
\newblock In \emph{Proceedings of the 7th ACM Conference on Learning @ Scale}, pages 341--344, 2020.

\bibitem{feng2009assistments}
M.~Feng, N.~Heffernan, and K.~Koedinger.
\newblock Addressing the assessment challenge with an online system that tutors as it assesses.
\newblock \emph{User Modeling and User-Adapted Interaction}, 19(3):243--266, 2009.

\bibitem{chang2015junyi}
H.-S.~Chang, H.-J.~Hsu, and K.-T.~Chen.
\newblock Modeling exercise relationships in e-learning: A unified approach.
\newblock In \emph{Proceedings of the 8th International Conference on Educational Data Mining (EDM)}, 2015.

\bibitem{chen2018prerequisite}
Y.~Chen, Q.~Liu, Z.~Huang, L.~Wu, E.~Chen, R.~Wu, Y.~Su, and G.~Hu.
\newblock Prerequisite-driven deep knowledge tracing.
\newblock In \emph{Proceedings of the IEEE International Conference on Data Mining (ICDM)}, pages 39--48, 2018.

\bibitem{ross2011dagger}
S.~Ross, G.~Gordon, and D.~Bagnell.
\newblock A reduction of imitation learning and structured prediction to no-regret online learning.
\newblock In \emph{Proceedings of the 14th International Conference on Artificial Intelligence and Statistics (AISTATS)}, pages 627--635, 2011.

\bibitem{torabi2018bc}
F.~Torabi, G.~Warnell, and P.~Stone.
\newblock Behavioral cloning from observation.
\newblock In \emph{Proceedings of the 27th International Joint Conference on Artificial Intelligence (IJCAI)}, pages 4950--4957, 2018.

\bibitem{jaderberg2017pbt}
M.~Jaderberg, V.~Dalibard, S.~Osindero, W.~M.~Czarnecki, J.~Donahue, A.~Razavi, O.~Vinyals, T.~Green, I.~Dunning, K.~Simonyan, C.~Fernando, and K.~Kavukcuoglu.
\newblock Population based training of neural networks.
\newblock \emph{arXiv preprint arXiv:1711.09846}, 2017.

\bibitem{salimans2017es}
T.~Salimans, J.~Ho, X.~Chen, S.~Szepesvari, and I.~Sutskever.
\newblock Evolution strategies as a scalable alternative to reinforcement learning.
\newblock \emph{arXiv preprint arXiv:1703.03864}, 2017.

\bibitem{kulesza2012dpp}
A.~Kulesza and B.~Taskar.
\newblock Determinantal point processes for machine learning.
\newblock \emph{Foundations and Trends in Machine Learning}, 5(2--3):123--286, 2012.

\bibitem{liu2023pykt}
Z.~Liu, Q.~Liu, J.~Chen, S.~Huang, and E.~Chen.
\newblock pyKT: A Python library to benchmark deep learning based knowledge tracing models.
\newblock In \emph{Proceedings of the 36th Conference on Neural Information Processing Systems (NeurIPS) Datasets and Benchmarks Track}, 2023.

\bibitem{mnih2015dqn}
V.~Mnih, K.~Kavukcuoglu, D.~Silver, A.~A.~Rusu, J.~Veness, M.~G.~Bellemare, A.~Graves, M.~Riedmiller, A.~K.~Fidjeland, G.~Ostrovski, et~al.
\newblock Human-level control through deep reinforcement learning.
\newblock \emph{Nature}, 518(7540):529--533, 2015.

\bibitem{mnih2016a2c}
V.~Mnih, A.~P.~Badia, M.~Mirza, A.~Graves, T.~Lillicrap, T.~Harley, D.~Silver, and K.~Kavukcuoglu.
\newblock Asynchronous methods for deep reinforcement learning.
\newblock In \emph{Proceedings of the 33rd International Conference on Machine Learning (ICML)}, pages 1928--1937, 2016.

\bibitem{liu2019ekt}
Q.~Liu, Z.~Huang, Y.~Yin, E.~Chen, H.~Xiong, Y.~Su, and G.~Hu.
\newblock EKT: Exercise-aware knowledge tracing for student performance prediction.
\newblock \emph{IEEE Transactions on Knowledge and Data Engineering}, 33(1):100--115, 2019.

\bibitem{zhu2020graphkt}
J.~Zhu, Y.~Ma, Q.~Li, and J.~Wang.
\newblock Knowledge tracing with structure-aware gated graph neural networks.
\newblock In \emph{Proceedings of the 29th ACM International Conference on Information and Knowledge Management (CIKM)}, 2020.

\bibitem{nakagawa2019gkt}
H.~Nakagawa, Y.~Iwasawa, and Y.~Matsuo.
\newblock Graph-based knowledge tracing: Modeling student proficiency using graph neural network.
\newblock In \emph{Proceedings of the IEEE/WIC/ACM International Conference on Web Intelligence}, pages 156--163, 2019.

\bibitem{khadka2018erl}
S.~Khadka and K.~Tumer.
\newblock Evolution-guided policy gradient in reinforcement learning.
\newblock In \emph{Advances in Neural Information Processing Systems (NeurIPS)}, pages 1196--1208, 2018.

\bibitem{rajeswaran2018dexterous}
A.~Rajeswaran, V.~Kumar, A.~Gupta, G.~Vezzani, J.~Schulman, E.~Todorov, and S.~Levine.
\newblock Learning complex dexterous manipulation with deep reinforcement learning and demonstrations.
\newblock In \emph{Proceedings of Robotics: Science and Systems (RSS)}, 2018.

\bibitem{nair2018demonstrations}
A.~Nair, B.~McGrew, M.~Andrychowicz, W.~Zaremba, and P.~Abbeel.
\newblock Overcoming exploration in reinforcement learning with demonstrations.
\newblock In \emph{IEEE International Conference on Robotics and Automation (ICRA)}, pages 6292--6299, 2018.

\bibitem{li2024privileged}
Q.~Li, W.~Xia, L.~Yin, J.~Jin, and Y.~Yu.
\newblock Privileged knowledge state distillation for reinforcement learning-based educational path recommendation.
\newblock In \emph{Proceedings of the 30th ACM SIGKDD Conference on Knowledge Discovery and Data Mining (KDD)}, 2024.

\end{thebibliography}

\end{document}
